package com.cisco;

import io.dropwizard.Application;
import io.dropwizard.setup.Environment;

public class RyanApplication extends Application<RyanConfig> {
    @Override
    public void run(RyanConfig ryanConfig, Environment environment) throws Exception {
        
    }
}
